# You can generate one with the command: openssl rand -hex 32
AUTH_SECRET=your-super-secret-auth-key-change-this

# Optional: Override default port (defaults to 7755)
# Only useful if you have any need for the internal
#   container port to be different
# PORT=7755

# Docker users: Leave as 0.0.0.0 (required for container networking)
# Native deployment: Can use 127.0.0.1 for localhost-only access
# If you don't understand any of this, leave it be
# HOST=0.0.0.0